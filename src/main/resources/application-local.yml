spring:
  kafka:
    bootstrap-servers: kafka:9092
  data:
    mongodb:
      host: localhost
      port: 27017
      username: dictuser
      password: test
      database: dictionary
  elasticsearch:
    uris: http://localhost:9200
  neo4j:
    uri: bolt://localhost:7687

    authentication:
      username: neo4j
      password: neo4jTtk-

logging:
  level:
    org:
      springframework:
        data:
          elasticsearch: DEBUG